const card2 = document.querySelectorAll('.card');
console.log(card2);

const form = document.querySelectorAll('#formulario');
console.log(form);

// si no existe
const no = document.querySelectorAll('#no-existe');
console.log(no);